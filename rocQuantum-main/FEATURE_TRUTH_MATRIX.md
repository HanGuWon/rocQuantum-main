# Feature Truth Matrix

Status legend: `IMPLEMENTED`, `PARTIAL`, `STUB`, `ABSENT`, `UNKNOWN`

Recent adapter note: Qiskit native Sampler and Estimator parameter batches now keep no-op initial `reset` instructions in the batched simulator path instead of falling back to per-parameter replay, Qiskit `u1` / `u2` / `u3` now lower through exact native single-qubit decompositions, and Qiskit `cs` / `csdg` / `csx` / `cu1` / `cu3` / `cu` now lower through exact native gate decompositions instead of dense controlled matrices, with `u1` / `u2` / `u3` / `cu1` / `cu3` / `cu` parameter sweeps staying batched. Runtime reset after prior operations remains a shot-trajectory-only path. PennyLane native adjoint payloads now include `Rot` / `CRot`, `PhaseShift` / controlled-phase, `MultiRZ`, `PauliRot`, Ising, `SingleExcitation` plus/minus, `DoubleExcitation` plus/minus, `PSWAP`, `FermionicSWAP`, and `OrbitalRotation` lowering into the root binding's RX/RY/RZ/P/CRX/CRY/CRZ/CP path, with explicit derivative scales for decomposed half-/eighth-angle gates.

| Area | Feature | Current Status | Evidence | Native Or Fallback | Comparison Target | Impact If Missing | Priority |
| --- | --- | --- | --- | --- | --- | --- | --- |
| Build / Packaging | Native CMake build root | IMPLEMENTED | `CMakeLists.txt`; CMake `3.21`; `find_package(hip CONFIG REQUIRED)`, `roc::rocblas`, `roc::rocsolver`, `hip::host`, component subdirectories | Native | cuQuantum / CUDA-Q | Required for all native use | P0 |
| Build / Packaging | Coherent Python isolated build contract | PARTIAL | `pyproject.toml` includes canonical `rocq`, `rocquantum`, Qiskit provider, and PennyLane plugin packages plus framework entry points; sub-integration `setup.py` files remain separate compatibility installers | Packaging only | CUDA-Q | Main install path is clearer, but release packaging still needs native ROCm wheel validation | P0 |
| Build / Packaging | Installable CMake package export | PARTIAL | `install(EXPORT rocQuantumTargets ...)`, installed public/component headers, generated `rocQuantumConfig.cmake` / version files, and `cmake/rocQuantumConfig.cmake.in` using ROCm config-package dependency names | Native install/export partial | cuQuantum | Downstream package shape exists, but native install-tree consumer validation is still required | P1 |
| Build / Packaging | Active `_rocq_hip_backend` CMake path | IMPLEMENTED | Top-level `CMakeLists.txt` adds `python/rocq`; `python/rocq/CMakeLists.txt` owns `_rocq_hip_backend` and links `hipStateVec`, `rocqsim_tensornet`, and `pybind11::module`; install rules include the target | Native Python extension build | CUDA-Q | Legacy Python native backend is active in the default bindings build; broader runtime unification remains separate work | P0 |
| Build / Packaging | Windows helper scripts aligned with current repo | IMPLEMENTED | `build.bat` and `build_rocq.bat` derive the project root from `%~dp0` and pass `CMAKE_HIP_ARCHITECTURES` to the root CMake build; `tests/test_framework_integration_contract.py` rejects stale `rocQuantum-1` and `AMDGPU_TARGETS` usage | Script-only source contract | cuQuantum | Windows helper scripts now match the current repo layout and root CMake GPU-architecture option; native Windows ROCm build validation is still environment-dependent | P1 |
| Build / Packaging | Explicit GPU architecture policy | IMPLEMENTED | Root `CMakeLists.txt` defaults `CMAKE_HIP_ARCHITECTURES` to `gfx950;gfx942;gfx90a`; CI narrows older ROCm lane to `gfx942;gfx90a` | Native build policy | cuQuantum | Users still need to override this for Radeon/workstation targets | P1 |
| CI / Release | ROCm CI build matrix | PARTIAL | `.github/workflows/rocm-linux-build.yml`; ROCm `6.2.2` and experimental `7.2.4` | Native CI | CUDA-Q / cuQuantum | Limited support proof | P1 |
| CI / Release | Runtime GPU regression coverage | PARTIAL | Workflow runs TensorNet smoke plus release benchmark registry artifacts for statevec, distributed reductions, TensorNet, and DensityMat | Native CI | cuQuantum | Benchmark artifacts make gaps visible, but full ROCm runtime coverage still depends on self-hosted GPU runners | P1 |
| CI / Release | Release benchmark artifacts | PARTIAL | `benchmarks/benchmark_manifest.json`; `benchmarks/run_release_benchmarks.py`; CI uploads `benchmark-artifacts` JSON | Native CI | CUDA-Q / cuQuantum | Missing binaries/devices are explicit skipped artifacts; thresholds and historical trend gates are not implemented yet | P1 |
| CI / Release | Python import / packaging smoke | IMPLEMENTED | Workflow runs `pip install -e .`, `import rocq; import rocquantum`, `python -m rocq_cli --help` | Python packaging | CUDA-Q | Basic developer UX covered | P1 |
| Compiler / Runtime | QIR emission from `rocqCompiler` | PARTIAL | `rocqCompiler/MLIRCompiler.cpp::emit_qir`; `createQuantumToSimulatorPass`; `createSimulatorToQIRPass` | Native codegen | CUDA-Q | Compiler parity claim is overstated | P0 |
| Compiler / Runtime | LLVM lowering integration | PARTIAL | `MLIRCompiler.cpp::emit_qir` calls `translateModuleToLLVMIR`, but pass coverage is incomplete | Native codegen | CUDA-Q | End-to-end compile story not credible | P0 |
| Compiler / Runtime | `compile_and_execute()` supported subset | PARTIAL | `rocqCompiler/MLIRCompiler.cpp::compile_and_execute` parses MLIR, dispatches qalloc/H/X/Y/Z/S/Sdg/T/Tdg, CNOT/CZ/SWAP/CCX/MCX/CSWAP, RX/RY/RZ/P, and CRX/CRY/CRZ/CP to `QuantumBackend`, and returns a statevector; unsupported ops raise diagnostics | Native backend execution for narrow subset | CUDA-Q | Removes the hard stub and aligns compiler execution with more of the existing HIP backend gate table, but remains far from compiler/runtime parity | P0 |
| Compiler / Runtime | Direct simulator execution via top-level `rocq` | IMPLEMENTED | `rocq/kernel.py::QuantumKernel.execute`; `rocq/backends.py`; `rocquantum/src/simulator.cpp` | Native when bindings available | CUDA-Q | Core local execution works | P0 |
| Compiler / Runtime | Legacy `python/rocq build()` and execute | PARTIAL | `python/rocq/api.py::build`; conceptual MLIR plus Python-side circuit replay | Mixed: conceptual MLIR + direct execution | CUDA-Q | Users may mistake it for compiler execution | P0 |
| Compiler / Runtime | One unified compiler/runtime stack | ABSENT | Divergent `rocqCompiler/*` and `rocquantum/include/src/rocqCompiler/*` trees | N/A | CUDA-Q | Architecture confusion | P0 |
| Compiler / Runtime | Adjoint generation in shipped path | PARTIAL | `python/rocq/api.py::adjoint`; `python/rocq/bindings.cpp` binds `run_adjoint_generation_pass`; root build now includes the legacy `_rocq_hip_backend` path | Partial legacy path | CUDA-Q | Advanced compiler story remains fragmented and not equivalent to CUDA-Q compiler/runtime integration | P2 |
| Compiler / Runtime | Mid-circuit measurement and classical feedback | PARTIAL | `integrations/qiskit-rocquantum-provider/qiskit_rocquantum_provider/backend.py::_apply_circuit_trajectory` executes Qiskit shot trajectories with mid-circuit `measure`, tuple/register `if_else`, finite `for_loop`, bounded `while_loop`, loop-local `break_loop` / `continue_loop`, and register/bit `switch_case` through `QuantumSimulator.measure_qubit()` | Native shot trajectory for Qiskit sampling | CUDA-Q | Hybrid algorithm scope is partially unblocked, but statevector/estimator dynamic semantics remain missing | P0 |
| State Vector | Single-qubit named gates (`X/Y/Z/H/S/Sdg/T/Tdg/Rx/Ry/Rz/P`) | IMPLEMENTED | `rocquantum/src/hipStateVec/hipStateVec.cpp`; backend bindings in `python/rocq/bindings.cpp` | Native HIP kernels | cuStateVec / CUDA-Q | Core simulator requirement | P0 |
| State Vector | Two-qubit named gates (`CNOT/CZ/SWAP`) | IMPLEMENTED | `hipStateVec.cpp`; `rocquantum/src/simulator.cpp::apply_gate` | Native HIP kernels | cuStateVec / CUDA-Q | Core simulator requirement | P0 |
| State Vector | Controlled rotations and phase (`CRX/CRY/CRZ/CP`) | IMPLEMENTED | `hipStateVec.cpp`; `python/rocq/api.py::crx/cry/crz/cp` | Native HIP kernels | cuStateVec / CUDA-Q | Variational circuits need this | P0 |
| State Vector | Multi-controlled X / Toffoli backend support | IMPLEMENTED | `python/rocq/api.py::ccx` enqueues `MCX`; `hipStateVec.cpp` contains `rocsvApplyMultiControlledX`; `QuantumSimulator::apply_gate` routes `MCX` / `CCX` / `TOFFOLI` | Native HIP backend | cuStateVec / CUDA-Q | Needed for parity claims | P0 |
| State Vector | `CSWAP` / Fredkin backend support | IMPLEMENTED | `python/rocq/api.py::cswap`; `hipStateVec.cpp::rocsvApplyCSWAP`; `apply_CSWAP_kernel` | Native HIP backend | cuStateVec / CUDA-Q | Needed for parity claims | P0 |
| State Vector | Generic matrix application | PARTIAL | `hipStateVec.cpp::rocsvApplyMatrix`; 1-4 target HIP fast path; `ROCQ_ALLOW_HOST_MATRIX_FALLBACK` gates slow fallback | Native fast path plus explicit opt-in slow/debug fallback | cuStateVec / CUDA-Q | Remaining unsupported arities fail clearly instead of silently copying through host | P0 |
| State Vector | Generic controlled matrix application | PARTIAL | `hipStateVec.cpp::rocsvApplyControlledMatrix`; `QuantumSimulator::apply_controlled_matrix`; root `bindings.cpp` and `rocquantum.framework_runtime.apply_controlled_matrix`; Qiskit generic controlled unitary dispatch; PennyLane `ControlledQubitUnitary` dispatch; 1-control/1-target HIP fast path; `ROCQ_ALLOW_HOST_MATRIX_FALLBACK` gates slow fallback | Native fast path plus explicit opt-in slow/debug fallback | cuStateVec / CUDA-Q | Controlled-unitary breadth is incomplete, but public/framework paths can now avoid full dense controlled matrices for generic controlled base unitaries | P0 |
| State Vector | Destructive measurement | IMPLEMENTED | `python/rocq/bindings.cpp::measure`; `hipStateVec.cpp::rocsvMeasure` | Native HIP | cuStateVec / CUDA-Q | Core simulator requirement | P0 |
| State Vector | Sampling | IMPLEMENTED | `python/rocq/bindings.cpp::sample`; `hipStateVec.cpp::rocsvSample` | Native HIP | cuStateVec / CUDA-Q | Hybrid workflows need counts | P0 |
| State Vector | Selected-qubit probability vectors | IMPLEMENTED | `hipStateVec.cpp::rocsvProbabilities`; `accumulate_distributed_sample_probabilities_rccl`; `rocquantum::QuantumSimulator::probabilities`; root `bindings.cpp` and legacy `python/rocq/bindings.cpp` expose probability hooks; PennyLane runtime falls back only when native reports unsupported | Native HIP local-state marginal probability reduction plus optional RCCL local-domain distributed reduction | cuStateVec / PennyLane Lightning.GPU | Analytic `qml.probs()` no longer requires full statevector readback for supported local-state or RCCL local-domain distributed targets | P0 |
| State Vector | Native single-Pauli expectations | IMPLEMENTED | `python/rocq/bindings.cpp::get_expectation_value_x/y/z`; corresponding `rocsvGetExpectation...` calls | Native HIP | cuStateVec / CUDA-Q | Needed for VQE credibility | P0 |
| State Vector | Native Z-product expectation | IMPLEMENTED | `python/rocq/bindings.cpp::get_expectation_value_pauli_product_z` | Native HIP | cuStateVec / CUDA-Q | Needed for simple Hamiltonians | P0 |
| State Vector | Native generic Pauli-string expectation | IMPLEMENTED | `python/rocq/bindings.cpp::get_expectation_pauli_string` / `get_expectation_pauli_string_batch`; `hipStateVec.cpp::rocsvGetExpectationPauliString` / `rocsvGetExpectationPauliStringBatch` | Native HIP, including local batch states | cuStateVec / CUDA-Q | Needed for generic observables and batched variational sweeps | P0 |
| State Vector | Dense matrix expectation | PARTIAL | `hipStateVec.cpp::rocsvGetExpectationMatrix` / `rocsvGetExpectationMatrixBatch`; `expectation_matrix_host_fallback` / `expectation_matrix_batch_host_fallback`; `expectation_matrix_distributed_host_fallback`; `reduce_expectation_matrix_kernel`; `QuantumSimulator::expectation_matrix` / `expectation_matrix_moments` / `expectation_matrix_batch` / `expectation_matrix_moments_batch`; shared `rocquantum.framework_runtime.expectation_matrix` / `expectation_matrix_moments` / `expectation_matrix_moments_batch` statevector fallback for old bindings; root `bindings.cpp`; `python/rocq/bindings.cpp::get_expectation_matrix_batch`; PennyLane `qml.Hermitian` expval/var dispatch; Qiskit dense `Operator` direct/Estimator dispatch | Native HIP reduction for up to 4 target qubits, including local batch states; public dense-matrix moments currently compute `<M>` and `<M^2>` via the dense expectation hooks; larger local/batched dense expectations and distributed dense expectation have explicit slow/debug host fallback only; older bindings use shared runtime statevector fallback for correctness, with Hermitian variance fallback reading state once for mean and second moment | cuStateVec / PennyLane Lightning.GPU | Hermitian observables and supported Qiskit dense `Operator` observables no longer require mandatory adapter-specific fallback for supported local paths; fallback Hermitian variance avoids duplicate statevector readback, while large dense/distributed correctness fallback exists but not a performant ROCm-native reduction | P0 |
| State Vector | Sparse matrix moments | PARTIAL | `hipStateVec.cpp::rocsvGetSparseMatrixMoments` / `rocsvGetSparseMatrixMomentsBatch`; `sparse_matrix_moments_distributed_host_fallback`; `reduce_sparse_matrix_moments_kernel` / `reduce_sparse_matrix_moments_batch_kernel`; `QuantumSimulator::sparse_hamiltonian_moments` / `sparse_hamiltonian_moments_batch`; shared `rocquantum.framework_runtime.sparse_hamiltonian_moments` / `sparse_hamiltonian_moments_batch` statevector fallback for old bindings; root and legacy bindings; PennyLane `qml.SparseHamiltonian` expval/var dispatch | Native HIP CSR row reduction for local single-state and batched state vectors; distributed full-state CSR sparse moments have explicit slow/debug host fallback only; older local bindings use shared runtime statevector CSR fallback for correctness | cuStateVec / PennyLane Lightning.GPU | SparseHamiltonian expectation/variance no longer require mandatory adapter-specific fallback on supported native single-state or simple batch paths; distributed correctness fallback exists but not performant ROCm-native reduction | P0 |
| State Vector | Sparse matrix gate application | PARTIAL | `hipStateVec.cpp::rocsvApplySparseMatrix`; `apply_sparse_matrix_kernel`; `apply_sparse_matrix_distributed_host_fallback`; `QuantumSimulator::apply_sparse_matrix` / `ApplySparseMatrix`; root `bindings.cpp`; `rocquantum.framework_runtime.apply_sparse_matrix`; PennyLane sparse `qml.BlockEncode` dispatch | Native HIP CSR apply for local single-state, batched state vectors, and local-domain distributed slices; non-local distributed sparse apply is explicit slow/debug host fallback only; host statevector fallback remains for unsupported or old bindings | cuStateVec / PennyLane Lightning.GPU | Sparse `BlockEncode` avoids dense operator materialization and Python readback on supported HIP paths, while non-local distributed sparse correctness has an opt-in fallback; AMD GPU performance validation remains pending | P0 |
| State Vector | Gate fusion engine | PARTIAL | `rocquantum/src/hipStateVec/GateFusion.cpp`; canonical `rocq.backends.StateVectorBackend` and legacy `python/rocq.Circuit.flush()` opportunistically fuse supported CNOT-adjacent spans; unsupported queue entries return `NOT_IMPLEMENTED` instead of being dropped | Native in canonical and legacy paths for narrow patterns | cuStateVec / CUDA-Q | Broader fusion still leaves performance on the table, but the legacy path no longer always replays eligible spans gate by gate | P1 |
| State Vector | Stabilizer / tableau / Pauli propagation backend | ABSENT | No such backend found in repo | N/A | CUDA-Q | Scope gap vs broader simulator portfolio | P2 |
| Public C++ API | `QuantumSimulator` basic apply/measure/reset/statevector/probabilities | IMPLEMENTED | `include/rocquantum/QuantumSimulator.h`; `rocquantum/src/simulator.cpp`; `bindings.cpp`; `QuantumSimulator.measure_qubit()` exposes native state-collapsing measurement; `QuantumSimulator.reset_qubit()` uses native measurement collapse plus conditional X; `QuantumSimulator.probabilities()` wraps `rocsvProbabilities` | Native wrapper | CUDA-Q / cuStateVec / PennyLane Lightning.GPU | User-facing simulator works for basics, provides measure/reset primitives for framework shot trajectories, and exposes analytic probabilities without mandatory statevector readback | P0 |
| Public C++ API | `QuantumSimulator` native expectation APIs | PARTIAL | `QuantumSimulator::expectation_value`, `QuantumSimulator::expectation_pauli_string`, `QuantumSimulator::expectation_matrix`, `QuantumSimulator::expectation_matrix_moments`, `QuantumSimulator::expectation_matrix_batch`, `QuantumSimulator::expectation_matrix_moments_batch`, `QuantumSimulator::sparse_hamiltonian_moments`, `QuantumSimulator::sparse_hamiltonian_moments_batch`; root `bindings.cpp` exposes `expectation_value`, `expectation_pauli_string`, `expectation_matrix`, `expectation_matrix_moments`, `expectation_matrix_batch`, `expectation_matrix_moments_batch`, `sparse_hamiltonian_moments`, and `sparse_hamiltonian_moments_batch` | Native Pauli-string, dense matrix, dense matrix moments, local batched dense matrix and dense matrix moments, local single/batched CSR sparse moments, explicit large dense-expectation host fallback, explicit distributed dense-expectation host fallback, and explicit distributed sparse-moment host fallback through hipStateVec | CUDA-Q / PennyLane Lightning.GPU | Pauli, supported Hermitian, and supported SparseHamiltonian observable paths are public, but broader arbitrary-operator, performant distributed dense/sparse expectation, fused dense moment kernels, and adjoint-gradient support remain missing | P0 |
| Public C++ API | Public named `MCX` / `CSWAP` / controlled-unitary surface | PARTIAL | `QuantumSimulator::apply_gate` routes `MCX` / `CCX` / `TOFFOLI` through `rocsvApplyMultiControlledX` and `CSWAP` / `FREDKIN` through `rocsvApplyCSWAP`; `QuantumSimulator::apply_controlled_matrix` exposes generic all-one-control matrix application | Native named gates plus partial controlled-matrix surface | CUDA-Q | Public multi-control parity improved, but broader controlled-unitary breadth and ROCm E2E validation still lag | P1 |
| Public C++ API | Native full-state upload | IMPLEMENTED | `QuantumSimulator::set_statevector`; `bindings.cpp` exposes `QuantumSimulator.set_statevector`; `rocquantum.framework_runtime::set_statevector` wraps modern and legacy binding names | Native host-to-device upload | CUDA-Q / PennyLane Lightning state initialization | Enables framework initial-state paths without dense preparation matrices, though partial state overwrite remains matrix/fallback territory | P1 |
| Density Matrix | Density-matrix allocation, reset, and core gates | IMPLEMENTED | `rocquantum/src/hipDensityMat/hipDensityMat.cpp`; `CMakeLists.txt` builds `rocq_hip_density_mat` | Native HIP | cuDensityMat | Real base functionality | P0 |
| Density Matrix | Basic noise channels | IMPLEMENTED | `hipDensityMat.cpp` bit flip, phase flip, depolarizing, amplitude damping now share `apply_kraus_channel` | Native HIP | cuDensityMat / CUDA-Q | Required for noisy simulation story | P0 |
| Density Matrix | Generic single-qubit Kraus channel API | PARTIAL | `hipDensityMatChannel_t`; `hipDensityMatApplyChannel`; Python `DensityMatrixState.apply_channel()` | Native HIP per-Kraus application, single-qubit only | cuDensityMat | Real generic channel path exists but lacks multi-qubit channels and GPU-resident descriptor planning | P0 |
| Density Matrix | Native X/Y/Z and Z-product expectations | IMPLEMENTED | `hipDensityMat.cpp` expectation helpers; `rocquantum/utils/hamiltonian.py` uses them | Native HIP + host orchestration | cuDensityMat | Partial VQE/noise credibility | P0 |
| Density Matrix | Sampling from density-matrix backend | PARTIAL | `hipDensityMatSample`; `hipDensityMat.cpp::extract_density_diagonal_kernel`; Python `DensityMatrixState.sample()`; canonical `rocq.backends.DensityMatrixBackend.sample()` | Device-side density-diagonal extraction plus host-side shot drawing | cuDensityMat / CUDA-Q | Noisy shot workflows now avoid full density-matrix host transfer, but still lack GPU-resident RNG/CDF sampling | P1 |
| Density Matrix | Arbitrary observable support | PARTIAL | `rocquantum/utils/hamiltonian.py` uses basis changes over limited native ops | Mixed native + host orchestration | cuDensityMat / CUDA-Q | Observable story is narrower than advertised | P1 |
| Tensor Network | Pairwise contraction and greedy multi-tensor contraction | IMPLEMENTED | `rocquantum/src/hipTensorNet/hipTensorNet.cpp`; runtime regression in CI | Native HIP/BLAS | cuTensorNet | Core tensor-network primitive exists | P0 |
| Tensor Network | Tensor SVD | PARTIAL | `python/rocq/bindings.cpp::tensor_svd`; repo implementation is limited to current dtype path | Native for current path | cuTensorNet | Useful but narrower than docs may imply | P1 |
| Tensor Network | Pathfinder optimizer wiring | PARTIAL | `hipTensorNet.cpp` validates optimizer enum and exposes capability bits; GREEDY is available, METIS/KAHYPAR return `NOT_IMPLEMENTED` unless compiled in | Native guarded | cuTensorNet | Optimizer choices are explicit but still narrow | P1 |
| Tensor Network | Slicing support | PARTIAL | `memory_limit_bytes` and `num_slices` influence contraction planning cost; capability reports runtime slicing as unsupported | Planning-only partial | cuTensorNet | Large contractions still lack true sliced execution | P1 |
| Tensor Network | Dtype breadth | PARTIAL | TensorNet dtype is gated by build precision: C64 in default builds, C128 in `ROCQ_PRECISION_DOUBLE` builds; unsupported dtypes fail clearly | Native but build-scoped | cuTensorNet | No simultaneous runtime C64/C128 support yet | P1 |
| Distributed / Multi-GPU | Distributed handle and state allocation scaffolding | PARTIAL | `hipStateVec.cpp::rocsvGetDistributedInfo`, `rocsvAllocateDistributedState`, `rocsvInitializeDistributedState` | Native partial | cuStateVec / CUDA-Q | Base exists, but not enough for full claim | P0 |
| Distributed / Multi-GPU | Multi-GPU gate application across local-domain qubits | PARTIAL | `hipStateVec.cpp` local-domain distributed paths; explicit host fallback for non-local single/control/CNOT/CZ/generic matrix cases via `ROCQ_DISTRIBUTED_FALLBACK_MODE=host` | Native partial plus explicit slow/debug host fallback | cuStateVec / CUDA-Q | `multi_gpu=True` remains experimental and performance-sensitive | P0 |
| Distributed / Multi-GPU | Multi-GPU measurement | PARTIAL | `MULTI_GPU_GUIDE.md` and `hipStateVec.cpp` indicate local-domain measurement path only | Native partial | cuStateVec / CUDA-Q | Feature boundary must be explicit | P0 |
| Distributed / Multi-GPU | Multi-GPU sampling/probabilities | PARTIAL | Local-domain measured qubits can use RCCL `AllReduce(sum)` over outcome probabilities for `rocsvSample` and `rocsvProbabilities`; slice-domain measured bits remain unsupported | Native optional RCCL partial | cuStateVec / CUDA-Q | Sampling/probability vectors are not general distributed measurement yet | P0 |
| Distributed / Multi-GPU | Multi-GPU expectation reductions | PARTIAL | Local-domain Pauli expectation helpers can use RCCL `AllReduce(sum)` over per-rank reductions; slice-domain X/Y/global terms remain unsupported | Native optional RCCL partial | cuStateVec / CUDA-Q | Expectation coverage is useful but not complete | P0 |
| Distributed / Multi-GPU | Multi-node distribution | ABSENT | No MPI/multi-node runtime path found | N/A | cuQuantum Appliance / CUDA-Q mqpu | Out of scope today | P2 |
| Distributed / Multi-GPU | RCCL build wiring | PARTIAL | `rocquantum/src/hipStateVec/CMakeLists.txt` defines `ROCQ_HAVE_RCCL`; `hipStateVec.cpp` initializes/destroys communicators and gates fast paths behind RCCL readiness | Native optional | cuStateVec | Comm dependency is optional, not release-grade | P1 |
| Performance | Stream / async contract | PARTIAL | `scripts/check_async_contract.sh` in CI; `ROCQUANTUM_ENABLE_SYNC_DEBUG` option exists | Native partial | cuQuantum / CUDA-Q | Async behavior is not fully surfaced to users | P1 |
| Performance | rocBLAS / rocSOLVER usage | IMPLEMENTED | Root `CMakeLists.txt` finds `rocblas`/`rocsolver`; tensor-network path uses them | Native | cuQuantum | Core acceleration dependency present | P1 |
| Python API | Canonical top-level `rocq` execution | IMPLEMENTED | `rocq/kernel.py`; `rocq/backends.py` | Native when compiled backend available | CUDA-Q | Main user path works for circuit execution | P0 |
| Python API | Canonical top-level operator expectation API | PARTIAL | `rocq/operator.py::get_expectation_value` delegates to `rocq.observe()`; canonical state-vector and density-matrix expectation paths combine duplicate Pauli terms before backend expectation calls; `QuantumOperator.__sub__` / `__neg__` and `SumOperator.__add__` preserve scaled composite coefficients during Pauli-term expansion; numeric constants in `QuantumOperator` arithmetic expand to identity terms; Pauli-only `QuantumOperator.__mul__` distributes sum/product expressions and combines Pauli products with same-qubit phase rules; `_rocq_hip_backend` exposes `get_expectation_pauli_string` / `get_expectation_pauli_string_batch`; `HermitianOperator(matrix, targets=...)` reaches `python/rocq/bindings.cpp::get_expectation_matrix` / `rocsvGetExpectationMatrix`; `_rocq_hip_backend` also exposes `get_expectation_matrix_batch`; `SparseHamiltonianOperator(data, indices, indptr, shape)` reaches `get_sparse_matrix_moments` / `rocsvGetSparseMatrixMoments` on the state-vector backend, with `_rocq_hip_backend::get_sparse_matrix_moments_batch` available for local batched CSR readouts | Native for supported Pauli operators, local batched Pauli-string hooks, dense Hermitian matrices, local batched dense matrix hooks, explicit large dense-expectation host fallback, local single/batched full-state CSR sparse Hamiltonians, explicit distributed dense-expectation host fallback, and explicit distributed sparse-moment host fallback when bindings are present; mock/old-binding fallback is explicit | CUDA-Q | Pauli VQE path plus targeted dense-matrix and full-state sparse observables are usable, including duplicate-combined Pauli sums, numeric identity constants, expanded Pauli sum/product expressions, and subtraction-built Hamiltonians, but performant large/distributed dense/sparse expectations, broader operator algebra, and adjoint-gradient support remain limited | P0 |
| Python API | `python/rocq Circuit.expval()` | PARTIAL | `python/rocq/api.py::expval`; uses native backend expectation helpers for supported Pauli terms | Native for supported Pauli terms, binding-dependent | CUDA-Q | Legacy surface still diverges from canonical runtime | P0 |
| Python API | `python/rocq get_expval()` | PARTIAL | `python/rocq/api.py::get_expval`; shares the native Pauli expectation helper path | Native for supported Pauli terms, binding-dependent | CUDA-Q | Legacy surface still diverges from canonical runtime | P0 |
| Python API | `multi_gpu=True` story | PARTIAL | `python/rocq/api.py::Circuit(..., multi_gpu=True)` plus partial distributed backend | Native partial | CUDA-Q | Users can assume full distributed execution | P0 |
| Python API | Mock fallback state backends | PARTIAL | `rocq/backends.py::_MockStateVectorState`, `_MockDensityMatrixState` | Mock fallback | CUDA-Q | Good for dev smoke, bad for parity claims | P1 |
| Integration | PennyLane adapter | PARTIAL | `integrations/pennylane-rocq/pennylane_rocq/rocq_device.py`; shared `rocquantum.framework_runtime`; root and subpackage entry points expose `lightning.rocq`, `lightning.rocm`, `rocquantum.qpu`, and `rocq.pennylane`; `tests/test_qiskit_pennylane_installed_contract.py` verifies PennyLane 0.45 plugin loading, full-wire and marginal `qml.probs()` including native `QuantumSimulator.probabilities()` preference and unsupported-native fallback, finite-shot `qml.sample()` / `qml.counts()` / `qml.probs()` native-measure dispatch without statevector readback, batched `qml.counts(all_outcomes=True)` and multiple compatible finite-shot `qml.sample()` / `qml.counts()` / `qml.probs()` readouts through native batch measurement, shot-vector sampling, native Pauli/Hadamard/Projector/Hamiltonian expval and var dispatch without diagonalizing rotations, native dense-matrix expectation preference for supported `qml.Hermitian` expval/var, batched Hermitian expval/variance readouts for simple parameter sweeps, native CSR moments preference for `qml.SparseHamiltonian`, batched SparseHamiltonian expval/variance readouts for simple parameter sweeps, lazy analytic statevector readback, full-wire initial `qml.StatePrep` through native statevector upload, direct RX/RY/RZ/P/CRX/CRY/CRZ/CP/Rot/CRot dispatch, native `MultiControlledX` including open `control_values`, generic `ControlledQubitUnitary` controlled-matrix dispatch including open controls, `Toffoli`, and `CSWAP`, exact `CH` / `CY` / `CCZ` native decompositions, exact `ECR` / `ISWAP` / `PSWAP` / `SISWAP` / `SQISW` native decompositions, exact native `PhaseShift` / controlled-phase dispatch without dense controlled-phase matrices, with decomposition fallback and measurement-aware global-phase elision for non-state outputs, exact `SingleExcitation` / `SingleExcitationPlus` / `SingleExcitationMinus` / `DoubleExcitation` / `DoubleExcitationPlus` / `DoubleExcitationMinus` / `FermionicSWAP` / `OrbitalRotation` / `MultiRZ` / `IsingXX` / `IsingYY` / `IsingXY` / `IsingZZ` native decompositions, initial `qml.BasisState`, `qml.SparseHamiltonian` statevector CSR fallback when the native hook is unavailable, common matrix-gate fallback dispatch, device jacobian parameter-shift batching across direct jacobian, probability-vector jacobian, `compute_derivatives()`, and `execute_and_compute_derivatives()` paths, root binding adjoint-Jacobian dispatch for trainable RX/RY/RZ/P/CRX/CRY/CRZ/CP plus Pauli-term, dense Hermitian, and full-state CSR SparseHamiltonian observables, `Rot` / `CRot` lowering into primitive adjoint rotation payloads, `PhaseShift` / controlled-phase mapping into native adjoint phase payloads, and operation-local trainable parameter positions | Native sampling and compatible finite-shot batch sample/count/probability readouts, native probability-vector hook through `rocsvProbabilities` when supported, native dense Hermitian expectation through `rocsvGetExpectationMatrix` / `rocsvGetExpectationMatrixBatch` when supported, explicit distributed dense-expectation host fallback, native CSR sparse moments through `rocsvGetSparseMatrixMoments` / `rocsvGetSparseMatrixMomentsBatch` when supported, explicit distributed sparse-moment host fallback, native parametric and controlled-parametric rotations, native `P` / `CP` dispatch, lazy state/probability readback, native full-wire state upload, native all-one and open-control multi-control gates, native generic controlled-qubit-unitary dispatch through `apply_controlled_matrix`, exact controlled-Pauli, ECR, sqrt-swap, swap, fermionic-swap, orbital-rotation, phase, single-/single-phase-/double-/double-phase-excitation, CRot, and controlled-phase fallback decompositions via native gates, exact Ising XX/YY/XY/ZZ rotations through native CNOT/rotation sequences, measurement-aware skipping of unobservable global-phase matrices for non-state measurements, computational-basis Projector expectations via Pauli-Z product expansion, matrix fallback for remaining common one-/two-/three-/four-qubit PennyLane gates and partial state prep, native CSR moments plus statevector CSR fallback for `SparseHamiltonian`, batched device parameter-shift gradients for compatible single- and multi-circuit derivative requests including probability-vector Jacobians through `probabilities_batch()`, root binding adjoint Jacobian for trainable RX/RY/RZ/P/CRX/CRY/CRZ/CP with Pauli-term, dense Hermitian, and full-state CSR sparse observables plus clean fallback when unsupported payloads are reported, and analytic Pauli / Hadamard / Projector / Pauli-Hamiltonian expectation and variance when binding exposes `measure` / `apply_gate` / `apply_matrix` / `expectation_pauli_string`; statevector/matrix fallback for legacy bindings and unsupported Hermitian/non-Pauli observables | PennyLane Lightning.GPU / CUDA-Q plugin ecosystem | Lightning-style AMD GPU entry points, batched parameter-shift device gradients, variational Pauli-Hamiltonian path, and broader binding-level adjoint operation/observable support exist with fewer avoidable Python-level transfers, but shipping GPU-resident adjoint kernels for general operation payloads, performant distributed dense/sparse expectations, native ROCm sparse performance validation, and ROCm E2E performance parity are still missing | P1 |
| Integration | Cirq adapter | PARTIAL | `integrations/cirq-rocm/cirq_rocm/roc_quantum_simulator.py`; `_run()` prefers `QuantumSimulator.measure` per measurement key and keeps statevector sampling only as legacy fallback | Native sampling when binding exposes `measure`; host fallback | CUDA-Q plugin ecosystem | Adapter is still thin but samples through native runtime by default | P1 |
| Integration | Qiskit provider simulator adapter | PARTIAL | `integrations/qiskit-rocquantum-provider/qiskit_rocquantum_provider/backend.py`; `sampler.py`; `estimator.py`; `job.py`; shared `rocquantum.framework_runtime`; provider exposes native `RocQuantumSampler`, native `RocQuantumEstimator`, optional generic Qiskit wrapper fallbacks via `get_sampler(native=False)` / `get_estimator(native=False)`, finite `Target` capacity for transpilation, `crx` / `cry` / `crz`, `mcx`, and `reset` target operations, native `ccx` / `mcx` / `cswap` dispatch, exact direct open-control controlled-X/Y/Z/H and controlled rotation/phase decomposition, exact `ch` / `cy` / `ccz` / `dcx` / `ecr` controlled-Pauli decomposition, exact `rccx` / `rcccx` relative-phase decomposition, exact `iswap` decomposition, exact `sx` / `sxdg` / `tdg` / `u` single-qubit decomposition, native `p` / `cp` phase dispatch with exact decomposition fallback, exact `rxx` / `ryy` / `rzz` and single/commuting-sum `PauliEvolutionGate` native decompositions, full-wire initial `initialize()` / direct `prepare_state()` through native statevector upload with later state preparation kept out of the batch-upload path, runtime `reset` backend/native Sampler sampling through shot-by-shot `QuantumSimulator.reset_qubit()` trajectories, simple `if_test` / `if_else`, finite `for_loop`, bounded `while_loop`, loop-local `break_loop` / `continue_loop`, and `switch_case` backend/native Sampler sampling through shot-by-shot `QuantumSimulator.measure_qubit()` trajectories, matrix-fallback gates (`unitary`, partial `state_preparation`), direct `estimate_expectation()` and native `RocQuantumEstimator` pubs for `SparsePauliOp` / `SparseObservable` / `Pauli` / supported full-circuit, default low-index partial, and dimension-tagged or explicit-target non-low-index partial dense `Operator` observables, and dense batch helper coverage for estimator plans; `tests/test_qiskit_pennylane_installed_contract.py` verifies Qiskit 2.4 `BackendV2`/`Job`/primitive construction, `transpile(circuit, backend)`, native sampler run and parameter binding, native estimator run, parameter binding, Pauli observable-batch circuit reuse and duplicate-observable caching, single-qubit, controlled-Pauli/ECR, controlled-rotation, direct open-control including parametric controlled gates, direct PauliEvolution, relative-phase controlled, swap, native phase dispatch, XX/YY/ZZ-rotation, and multi-control native dispatch, matrix/partial-state-preparation fallback dispatch, default sampling without statevector readback, opt-in statevector result data, `save_statevector` marker handling, runtime-reset shot-by-shot sampling, simple if_else, finite for_loop, bounded while_loop, loop-local break/continue, and switch_case shot-by-shot sampling, statevector/estimator trajectory rejection, non-terminating control-flow rejection, repeated-measurement deduplication, padded duplicate Pauli term canonicalization, and native Pauli/dense direct expectation dispatch with fake native binding; `integrations/qiskit-rocquantum-provider/tests/test_backend.py` now runs without a native binding via fake simulator fallback | Native sampling plus native full-wire state upload, native single-qubit decompositions, native phase dispatch with decomposition fallback, controlled-Pauli/ECR gates, direct open-control gates including controlled rotations/phase, direct PauliEvolution gates for supported Pauli sums, relative-phase controlled gates, controlled rotations, swap decomposition, XX/YY/ZZ rotations, multi-control gates, runtime reset sampling via shot-by-shot reset trajectories, simple if_else, finite for_loop, bounded while_loop, loop-local break/continue, and switch_case sampling via shot-by-shot measure trajectories, matrix fallback for explicit unitary/partial state preparation, native Pauli-string expectation for supported Pauli observables, native dense-matrix expectation for supported dense `Operator` observables via direct `estimate_expectation()` and native Estimator dense Operator pubs, and dense batch plans through `QuantumSimulator.expectation_matrix_batch()` when supplied; Estimator reuses bound-circuit state across supported Pauli and dense Operator observable batches and caches canonical duplicates; Python result formatting | Qiskit primitives / CUDA-Q plugin ecosystem | Counts, transpilation, opt-in statevector result payload, native sampler/estimator paths, state upload, single-qubit, controlled-Pauli/ECR, direct open-control including parametric variants, PauliEvolution, relative-phase controlled, controlled-rotation, swap, native phase dispatch, XX/YY/ZZ-rotation, multi-control gate paths, runtime-reset and simple if_else/finite-for-loop/bounded-while-loop/break-continue/switch_case backend/native Sampler sampling paths, matrix/partial-state-preparation fallback path, deterministic Pauli-observable expectation paths, direct and Estimator full-circuit, default low-index partial, and dimension-tagged or explicit-target non-low-index partial dense Operator expectation paths, and helper-layer dense batch plans are covered; native ROCm E2E, statevector/estimator trajectory output, statevector/estimator dynamic circuits, plain compact dense Operator placement without dimension metadata or explicit target wrappers still defaults to low-index wires, and full primitive result parity is still missing | P1 |
| Integration | Integration tests are native ROCm E2E | ABSENT | `tests/test_pennylane_integration.py`, `tests/test_framework_integration_contract.py`, and `tests/test_qiskit_pennylane_installed_contract.py` cover adapter/source/current-framework contracts without a real ROCm binding | Mock/source/framework contract | CUDA-Q ecosystem | Integrations are API-proven but not runtime-proven on AMD GPU yet | P1 |
| Provider Backends | IonQ / Pasqal / Infleqtion / Rigetti / Quantinuum clients | PARTIAL | `rocquantum/backends/*.py`; concrete client logic exists, but verification is limited | Remote API clients | CUDA-Q platform connectors | Usable in parts, not uniformly proven | P1 |
| Provider Backends | Qristal local SDK bridge | PARTIAL | `rocquantum/backends/qristal.py` includes local-simulation style placeholder logic | Mixed / mock-like | CUDA-Q platform connectors | Local SDK story is not production-grade | P2 |
| Provider Backends | Skeleton vendors (`alice_bob`, `iqm`, `orca`, `qm`, `quera`, `seeqc`, `xanadu`) | ABSENT | Files contain `pass` / TODOs | Skeleton only | CUDA-Q platform connectors | Must not be presented as integrated | P0 |
| Higher-Level Libraries | VQE solver | PARTIAL | `rocquantum/solvers/vqe_solver.py` evaluates canonical `QuantumOperator` objectives through `rocq.observe()` and exposes parameter-shift / finite-diff gradient estimates; scaled/subtracted composite observables preserve coefficients; numeric constants and Pauli sum/product expressions in operator arithmetic expand to Pauli terms; single-argument vector ansatz kernels receive the parameter array instead of scalar splats | Experimental Python helper over native observe | CUDA-QX | Usable minimal VQE loop for supported Pauli, dense Hermitian, scaled/subtracted composite sums, numeric identity constants, Pauli sum/product Hamiltonian terms, vector-parameter QAOA ansatz objectives, and full-state sparse operators, not a robust solver library | P0 |
| Higher-Level Libraries | QAOA helper | PARTIAL | `rocquantum/solvers/qaoa.py` builds MaxCut-style H/ZZ/RX ansatz kernels, uses CNOT-RZ-CNOT cost phases with `-gamma * w`, and returns weighted `0.5 * w * (I - Zi Zj)` cost operators; `tests/test_cudaqx_layer_contract.py` verifies the QAOA vector ansatz can be evaluated by the VQE objective path | Experimental Python helper | CUDA-QX | Narrow but executable QAOA subset that can feed the experimental VQE objective helper with correct MaxCut cost offsets and matching cost-phase convention | P1 |
| Higher-Level Libraries | QEC framework | PARTIAL | `rocquantum/qec/framework.py::run_repetition_code_single_round` samples a 3-qubit repetition-code syndrome via canonical `rocq.sample()` | Experimental Python helper over native sampling | CUDA-QX | Single-round repetition-code demo exists; no full decoder/library stack | P1 |
| Higher-Level Libraries | CUDA-QX-style accelerated libraries | ABSENT | No robust decoders / solver libs / hybrid-library stack beyond the experimental VQE/QAOA/QEC subset | N/A | CUDA-QX | Major scope gap | P2 |
