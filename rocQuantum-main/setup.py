from __future__ import annotations

if __name__ == "__main__":
    raise SystemExit(
        "rocQuantum now builds through pyproject.toml with scikit-build-core. "
        "Use `pip install .` or `python -m build` from the repository root."
    )
